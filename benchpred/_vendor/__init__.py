"""Vendored third-party code used by benchpred methods.

Code under this package is copied (lightly trimmed) from external projects and
kept here so benchpred does not depend on those packages being installed /
importable.  Each module carries a provenance header naming its source.
"""
